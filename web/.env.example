# Base URL of the FastAPI backend the UI talks to.
# Local development default (used if this is unset):
VITE_API_BASE_URL=http://127.0.0.1:8000

# NEVER put LLM / embedding / vector-DB credentials here.
# The browser only ever calls FastAPI; all provider secrets stay
# server-side in the backend's own .env.
